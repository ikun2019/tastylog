<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">tastylog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a href="/search" class="nav-link">Search</a>
        </li>
      </ul>
      <% if (locals.user) { %>
        <form action="/account/logout" method="POST" class="form-inline my-2 my-lg-0">
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a href="#" class="nav-link dropdown-toggle" id="navbarDropdownMenulink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= (locals.user.name || '(表示名未設定)') %>
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdownMenulink">
                <a href="#" class="dropdown-item">ホーム</a>
                <a href="#" class="dropdown-item">マイ食レポ</a>
                <a href="#" class="dropdown-item">設定</a>
                <span class="dropdown-item">
                  <input type="submit" class="btn btn-outline success my-2 my-sm-0" value="ログアウト">
                </span>
              </div>
            </li>
          </ul>
        </form>
      <% } else { %>
        <a href="/account/login" class="btn btn-outline-success my-2 my-sm-0 mr-2">ログイン</a>
        <a href="#" class="btn btn-outline-success my-2 my-sm-0">新規登録</a>
      <% } %>
    </div>
  </div>
</nav>